<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".MainActivity"
    android:id="@+id/contentroot">


    <EditText
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:id="@+id/morsein"
        android:inputType="text"
        app:layout_constraintLeft_toLeftOf="@+id/msglist"
        app:layout_constraintRight_toLeftOf="@+id/convertbutton"
        app:layout_constraintBottom_toBottomOf="@+id/contentroot"
        tools:ignore="LabelFor"/>

    <Button
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:text="@string/convert"
        android:id="@+id/convertbutton"
        app:layout_constraintRight_toRightOf="@+id/msglist"
        app:layout_constraintBottom_toBottomOf="@+id/contentroot"/>

    <ListView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:id="@+id/msglist"
        android:dividerHeight="2dp"
        app:layout_constraintBottom_toTopOf="@+id/convertbutton"
        app:layout_constraintLeft_toLeftOf="@+id/contentroot"
        app:layout_constraintRight_toRightOf="@+id/contentroot"
        app:layout_constraintTop_toTopOf="@+id/contentroot"
        />


</android.support.constraint.ConstraintLayout>